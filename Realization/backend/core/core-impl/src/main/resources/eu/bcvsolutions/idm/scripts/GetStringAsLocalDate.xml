<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<!--
Converts String to Local Date as 'dd.MM.yyyy'.
-->
<script xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:noNamespaceSchemaLocation="IdmScript.xsd">
  <code>GetStringAsLocalDate</code>
  <name>GetStringAsLocalDate</name>
  <body><![CDATA[import org.joda.time.LocalDate;
import org.joda.time.format.DateTimeFormat;
import org.joda.time.format.DateTimeFormatter;

if (attributeValue == null) {
    return null;
}

DateTimeFormatter dateFormat = DateTimeFormat.forPattern(pattern);
LocalDate localDate = dateFormat.parseLocalDate(attributeValue);
return localDate;]]></body>
<type>groovy</type>
 <category>TRANSFORM_FROM</category>
 <description>Converts String to Local Date as patern.
 Input:
 String date -> attributeValue
 String (like "yyyy-MM-dd HH:mm:ss.S") -> patern</description>
</script>
